<h1>Starstriker's Retro Game Reviews!</h1>


<div class="game_list_header">
  <h2>Game List:</h2>
  <div class="game-list">
    <% @games.each do |game| %>
      <p class='animated slideInLeft'><%= link_to game.title, game %></p>
    <% end %>
  </div>
</div>
<div id="react" />
